-- Retornar o nome de todas as boias 
'/boiasNomes'

-- Retornar todos os dados de todas as boias
'/boias'

-- Retornar uma boia especifica, passando o nome como parametro
'/boia/<boiaNome>'

-- Retornar uma lista de anos cadastrados
'boiasAno'

--Retorna uma lista com todas as categorias de dados (ex WindSpeed, WindDirection)
'boiasCat'

-- Retorna lista com atributos especificos do objeto
'boia/<boiaNome>/<Cat>'

-- Retorna valores dentro de um range (maior, menor, igual)
'/boiasQuery/<Nome>/<Ano>/<X>/<Y>/<minX>/<maxX>/<minY>/<maxY>'


